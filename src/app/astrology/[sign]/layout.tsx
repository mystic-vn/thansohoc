'use client';

import MainLayout from '../../MainLayout';

export default function AstrologySignLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <MainLayout>{children}</MainLayout>;
} 